services:
  - type: web
    name: busha-spread-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python busha_spread_tracker.py
    envVars:
      - key: MID_PROVIDER
        value: cbn
      - key: BUSHA_API_KEY
        sync: false
